import pandas as pd

# If uploading manually:
from google.colab import files
uploaded = files.upload()  # Upload student_event_feedback_sample.csv

# Load into DataFrame
df = pd.read_csv(next(iter(uploaded)))

# Quick preview
df.head()
